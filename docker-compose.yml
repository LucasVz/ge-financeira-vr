version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: clinica-postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin_password
      POSTGRES_DB: clinica_db
    ports:
      - "5432:5432"